name: Github copilot Automation Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
  schedule:
    - cron: "30 2 * * *" # Runs at 02:30 AM UTC (8:00 AM IST)

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          playwright install --with-deps

      # Run Pytest with HTML + Allure (write outputs under reports/)
      - name: Run Pytest tests
        run: |
          pytest -v --html=reports/report.html --self-contained-html --alluredir=reports/allure-results

      - name: Verify Allure Results
        run: |
          echo "Checking reports/allure-results..."
          ls -R reports/allure-results || true
      # Install Allure CLI
      - name: Install Allure CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y default-jre
          curl -o allure.tgz -Ls https://github.com/allure-framework/allure2/releases/download/2.27.0/allure-2.27.0.tgz
          tar -zxvf allure.tgz
          sudo mv allure-2.27.0 /opt/allure
          sudo ln -s /opt/allure/bin/allure /usr/bin/allure

      # Generate Allure HTML Report (output under reports/)
      - name: Generate Allure Report
        run: |
          allure generate reports/allure-results --clean -o reports/allure-report --single-file || true

      # Upload Pytest HTML Report
      - name: Upload Pytest HTML Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pytest-report
          path: reports/report.html

      # Upload Allure Report (support directory or single-file outputs)
      - name: Upload Allure Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: |
            reports/allure-report/**
            reports/allure-report
            reports/allure-report.html
            reports/allure-report.zip
