When generating Pytest code:

- Use fixtures from conftest.py.
- Do not create browser/page inside tests.
- Use CLI-based config (--env, --browser).
- Write independent test cases.
- Follow AAA pattern (Arrange, Act, Assert).
- Add meaningful assertion messages.
- Avoid hardcoded values.
